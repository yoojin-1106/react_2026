-- =========================================================
-- 스토어드 프로시저 응용 문제 5개
-- 학습 내용: IF, CASE, WHILE, PREPARE, EXECUTE
-- 사용 데이터베이스: market_db
-- =========================================================


-- ---------------------------------------------------------
-- 문제 1. IF 문을 활용한 회원 인원수 판별
-- ---------------------------------------------------------
-- member 테이블에서 mem_id가 'BLK'인 회원의 mem_number 값을 조회하여
-- 변수에 저장하는 스토어드 프로시저 ifMemberProc를 작성하시오.
--
-- mem_number가 5 이상이면 '5명 이상인 그룹입니다.'를 출력하고,
-- 그렇지 않으면 '5명 미만인 그룹입니다.'를 출력하시오.
--
-- 작성 조건
-- 1) DROP PROCEDURE IF EXISTS를 사용할 것
-- 2) DECLARE로 정수형 변수를 선언할 것
-- 3) SELECT ... INTO를 사용할 것
-- 4) IF ~ ELSE 문을 사용할 것
-- 5) 프로시저 생성 후 CALL 문으로 실행할 것





-- ---------------------------------------------------------
-- 문제 2. CASE 문을 활용한 회원 등급 판정
-- ---------------------------------------------------------
-- 정수형 변수 purchaseAmount에 1200을 저장한 후,
-- 구매 금액에 따라 회원 등급을 판정하는 스토어드 프로시저
-- caseGradeProc를 작성하시오.
--
-- 등급 기준
-- 1500 이상 : '최우수고객'
-- 1000 이상 : '우수고객'
-- 500 이상  : '일반고객'
-- 500 미만  : '새싹고객'
--
-- 마지막에는 다음 형식으로 결과를 출력하시오.
-- 구매금액==>1200
-- 회원등급==>우수고객
--
-- 작성 조건
-- 1) DECLARE로 구매 금액 변수와 등급 변수를 선언할 것
-- 2) CASE 문을 사용할 것
-- 3) CONCAT() 함수를 사용할 것
-- 4) 프로시저 생성 후 CALL 문으로 실행할 것





-- ---------------------------------------------------------
-- 문제 3. WHILE 문을 활용한 홀수 합계 구하기
-- ---------------------------------------------------------
-- 1부터 50까지의 숫자 중 홀수만 더한 결과를 출력하는
-- 스토어드 프로시저 whileOddProc를 작성하시오.
--
-- 작성 조건
-- 1) 반복에 사용할 변수 i와 합계를 저장할 변수 hap을 선언할 것
-- 2) i의 시작값은 1, hap의 시작값은 0으로 설정할 것
-- 3) WHILE 문을 사용할 것
-- 4) IF 문과 나머지 연산자(%)를 사용하여 홀수만 더할 것
-- 5) 반복이 끝난 후 '1부터 50까지 홀수의 합 ==>'과 합계를 출력할 것
-- 6) 프로시저 생성 후 CALL 문으로 실행할 것





-- ---------------------------------------------------------
-- 문제 4. WHILE, ITERATE, LEAVE를 활용한 반복 제어
-- ---------------------------------------------------------
-- 1부터 100까지 증가하면서 다음 조건에 따라 합계를 구하는
-- 스토어드 프로시저 whileControlProc를 작성하시오.
--
-- 조건
-- 1) 3의 배수는 합계에서 제외할 것
-- 2) 합계가 500을 초과하면 즉시 반복을 종료할 것
-- 3) 3의 배수를 만났을 때는 ITERATE를 사용할 것
-- 4) 합계가 500을 초과했을 때는 LEAVE를 사용할 것
-- 5) WHILE 문에 label을 지정할 것
-- 6) 마지막에는 현재 i 값과 최종 합계를 함께 출력할 것
--
-- 출력 예시 형식
-- 종료된 숫자==>?
-- 최종 합계==>?





-- ---------------------------------------------------------
-- 문제 5. 동적 SQL을 활용한 조건 검색
-- ---------------------------------------------------------
-- 사용자 변수 @searchAddr에 '서울'을 저장한 후,
-- member 테이블에서 addr 값이 @searchAddr와 같은 회원만 조회하는
-- 동적 SQL을 작성하시오.
--
-- 조회할 열
-- mem_id, mem_name, addr
--
-- 작성 조건
-- 1) SET 문으로 @searchAddr 변수에 값을 저장할 것
-- 2) PREPARE 문에서 ? 자리표시자를 사용할 것
-- 3) EXECUTE ... USING 문을 사용할 것
-- 4) 실행 후 DEALLOCATE PREPARE로 준비된 문장을 해제할 것
-- 5) 스토어드 프로시저가 아니라 일반 동적 SQL 형태로 작성할 것




